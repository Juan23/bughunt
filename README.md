# Review Skills

This is a standalone plugin package for [github.com/Juan23/bughunt](https://github.com/Juan23/bughunt). Install it as a marketplace/plugin, then invoke `$bughunt` explicitly for a one-shot daily pass. It is read-only, has no machine-global toggle, and never imposes a per-task gate.
